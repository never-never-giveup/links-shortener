# CLAUDE.md

**Первым шагом прочитай `AGENTS.md`** — это главный файл с правилами: язык
общения (включая русское описание коммитов), главные правила, реакция на
ошибки (Identify → Assess → Communicate → Solutions), именование веток,
commit-теги и работа с pre-commit хуками.

`AGENTS.md` — карта по остальным правилам (`agents/`) и алгоритмам
(`skills/`): читай оттуда только те файлы, что нужны под текущую задачу.

Не начинай реальную работу, не прочитав `AGENTS.md`.
